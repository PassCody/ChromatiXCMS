'use strict';(function () {function uhrzeit() {var jetzt = new Date(),y = jetzt.getFullYear(),mo = jetzt.getMonth() + 1,da = jetzt.getDate(),h = jetzt.getHours(),mi = jetzt.getMinutes(),s = jetzt.getSeconds();da = fuehrendeNull(da);mo = fuehrendeNull(mo);mi = fuehrendeNull(mi);s = fuehrendeNull(s);document.getElementById('date_time').innerHTML = '&emsp;' + da + '.' + mo + '.' + y + ' - ' + h + ':' + mi + ':' + s + '';setTimeout(uhrzeit, 500);}function fuehrendeNull(zahl) {zahl = (zahl < 10 ? '0' : '') + zahl;return zahl;}document.addEventListener('DOMContentLoaded', uhrzeit);}());